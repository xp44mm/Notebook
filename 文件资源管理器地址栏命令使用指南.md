# 文件资源管理器地址栏命令使用指南

在 Windows 文件资源管理器的地址栏中，除了输入常规的路径外，还可以直接运行一些特殊的命令来快速访问系统功能或工具。以下是常见的可用命令和用法：

---

### **1. 系统工具命令**
- **`cmd`**
  打开当前目录的命令提示符（以当前文件夹为工作路径）。
- **`powershell`** 
  在当前目录打开 PowerShell。
- **`wt`**（需安装 Windows Terminal） 
  在当前目录打开 Windows Terminal。
- **`control`** 
  打开控制面板。
- **`msconfig`** 
  打开系统配置工具（启动项管理）。
- **`regedit`** 
  打开注册表编辑器。
- **`devmgmt.msc`** 
  打开设备管理器。
- **`services.msc`** 
  打开服务管理窗口。
- **`taskmgr`** 
  打开任务管理器。
- **`mstsc`** 
  打开远程桌面连接。
- **`cleanmgr`** 
  打开磁盘清理工具。
- **`compmgmt.msc`** 
  打开计算机管理（包含磁盘、服务、事件查看器等）。

---

### **2. 系统文件夹快捷命令**
- **`.`** 
  当前目录（输入后按回车无效果，但可用于路径拼接）。
- **`..`** 
  返回上一级目录（等同于点击导航栏的“上一步”）。
- **`\\`** 
  打开网络邻居（查看局域网共享）。
- **`shell:startup`** 
  打开当前用户的启动文件夹（开机自启项存放位置）。
- **`shell:desktop`** 
  直接进入桌面文件夹。
- **`shell:downloads`** 
  打开当前用户的“下载”文件夹。
- **`shell:recent`** 
  查看最近访问的文件记录。

---

### **3. 特殊协议或路径**
- **`%temp%`** 
  打开系统临时文件夹。
- **`%appdata%`** 
  打开当前用户的 `AppData\Roaming` 文件夹。
- **`%localappdata%`** 
  打开 `AppData\Local` 文件夹。
- **`%windir%`** 
  打开 Windows 系统目录（通常是 `C:\Windows`）。
- **`ms-settings:`** 
  打开 Windows 设置（例如 `ms-settings:network` 直接跳转网络设置）。

---

### **4. 网络相关**
- **`ftp://example.com`** 
  访问 FTP 服务器（需替换为实际地址）。
- **`\\192.168.x.x`** 
  访问局域网共享文件夹（需替换为实际 IP）。

---

### **5. 其他实用命令**
- **`notepad`** 
  打开记事本。
- **`calc`** 
  打开计算器。
- **`explorer`** 
  重新打开文件资源管理器窗口。
- **`shutdown /s /t 0`** 
  立即关机（需谨慎使用）。

---

### **注意事项**
1. **部分命令需要管理员权限**（如 `regedit`、`devmgmt.msc`），普通用户运行时可能会提示输入密码。
2. **路径补全**：输入部分路径后按 `Tab` 键可自动补全（例如输入 `C:\Pro` 后按 `Tab`）。
3. **历史记录**：地址栏会保存之前输入的命令或路径，可通过下拉箭头查看。

这些命令可以大幅提升操作效率，尤其是在需要快速调用系统工具时。